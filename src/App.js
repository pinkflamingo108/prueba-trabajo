import React from "react";
import Barra from "./Barra";
import DisplayInfo from "./DisplayInfo";

const App = (props) => {
  return (
    <div>
      <Barra />
      <DisplayInfo />
    </div>
  );
};

export default App;
